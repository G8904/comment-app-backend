{
  "version": "1.0.0",
  "main": "main.ts",
  "scripts": {
    "start": "nest start",
    "start:dev": "nest start --watch",
    "build": "npx nest build"
  },
  "dependencies": {
    "@nestjs/cli": "^10.0.0"
  }
}
